# Hoist Babel packages to root for AdminJS bundler compatibility
# AdminJS's bundler resolves Babel plugins from the workspace root,
# but pnpm's strict node_modules structure isolates them to the package level
public-hoist-pattern[]=@babel/*
