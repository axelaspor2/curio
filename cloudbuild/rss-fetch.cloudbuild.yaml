substitutions:
  _REGION: asia-northeast1
  _ARTIFACT_REGISTRY: curio

options:
  logging: CLOUD_LOGGING_ONLY

steps:
  - id: 'build'
    name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--cache-from'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REGISTRY}/curio-rss-fetch:latest'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REGISTRY}/curio-rss-fetch:${SHORT_SHA}'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REGISTRY}/curio-rss-fetch:latest'
      - '-f'
      - 'apps/jobs/rss-fetch.Dockerfile'
      - 'apps'

  - id: 'push'
    name: 'gcr.io/cloud-builders/docker'
    args: ['push', '--all-tags', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REGISTRY}/curio-rss-fetch']
    waitFor: ['build']

  - id: 'deploy'
    name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        sed 's|\$${IMAGE}|${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REGISTRY}/curio-rss-fetch:${SHORT_SHA}|g' \
          infra/cloudrun/curio-rss-fetch.yaml | gcloud run jobs replace - --region=${_REGION}
    waitFor: ['push']

timeout: 600s
